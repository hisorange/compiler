import { Signale } from 'signale';

/**
 * Package wide logger interface, using the awesome
 * Signale package for base.
 *
 * @export
 * @interface ILogger
 * @extends {Signale}
 */
export interface ILogger extends Signale {}
